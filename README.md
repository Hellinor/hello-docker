# hello-docker

Build an Ubuntu image which, when the container starts up, runs a script that displays a greeting and the current date.

## what it does 

Display a greeting and the current date

## how to build & run

```bash
docker build -t hello-docker .
docker run hello-docker
```
